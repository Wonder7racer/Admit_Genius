# 启用链式调用
lombok.accessors.chain=true

# 生成所有字段的getter
lombok.getter.noIsPrefix=false

# 生成所有字段的setter
lombok.setter.flagUsage=allow

# 添加@lombok.Generated注解
lombok.addLombokGeneratedAnnotation=true

# 在IDEA中启用配置
config.stopBubbling=true
